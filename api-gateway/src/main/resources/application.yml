
server:
  port: 9000
spring:
  application:
    name: api-gateway

  datasource:
    url: jdbc:mysql://localhost:3306/flashsalegateway
    username: root
    password: ucd21213011.

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
    gateway:
      discovery:
        locator:
          enabled: true # 让gateway可以发现nacos中的微服务



